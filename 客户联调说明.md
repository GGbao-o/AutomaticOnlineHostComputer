# Automatic Online Host Computer 客户联调说明

> 本说明用于现场测试、设备联调和文件依赖确认。
> 这份文档只描述如何运行、如何准备文件、如何排查问题，不修改界面与业务代码。

---

## 1. 项目定位
这个项目是一个 WPF .NET 8 的自动线上位机前端骨架。

当前客户现场主要用途：
- 先确认程序能正常启动
- 再确认各类设备的通信文件是否准备完整
- 再确认现场人员能按协议进行连通测试

项目当前强调的是：
- 主页面布局
- 设备联调入口
- 后续逐步增加业务逻辑

---

## 2. 打开方式

### 方式一：Visual Studio 2022
1. 打开 `AutomaticOnlineHostComputer.sln`
2. 选择启动项目 `AutomaticOnlineHostComputer`
3. 按 `F5` 运行

### 方式二：直接运行
如果已经打包完成，可以直接双击运行生成的 `AutomaticOnlineHostComputer.exe`。

---

## 3. 客户现场需要先准备什么

### 3.1 程序文件
客户机器上需要有以下基础文件：
- `AutomaticOnlineHostComputer.exe`
- `AutomaticOnlineHostComputer.dll`
- `AutomaticOnlineHostComputer.deps.json`
- `AutomaticOnlineHostComputer.runtimeconfig.json`

### 3.2 依赖文件
不同设备会依赖不同厂家的 SDK 或 DLL。

#### 沈阳斜床 / FANUC FOCAS
通常需要：
- `FWLIB32.dll`
- 厂家提供的示例代码或说明
- 设备 IP、端口、宏变量表

#### 双头镗 / Syntec Remote CNC
通常需要：
- `Syntec.OpenCNC.dll`
- 厂家 SDK 说明
- 设备 IP
- 宏变量范围表
- R 区说明（如果设备使用）

#### 天车 / 机械手 / 汇川 PLC
通常需要：
- 对应 PLC 的通讯协议说明
- IP、端口、站号
- 寄存器或点位表
- 如有必要，控制卡厂家的 DLL

---

## 4. 常见文件缺失的判断方法

### 4.1 如果报“找不到文件”
通常是以下情况之一：
- DLL 没放到程序目录
- DLL 名称写错
- 程序找的是 x86 版本，但实际只有 x64
- 程序目录和运行目录不一致

### 4.2 如果报“无法加载 DLL”
通常是：
- DLL 位数不匹配
- DLL 依赖的其它文件缺失
- 目标机器没有对应运行库

### 4.3 如果报“入口点不存在”
通常是：
- DLL 版本不对
- 厂家 SDK 升级后接口变了
- 调用方式与厂家文档不一致

---

## 5. 沈阳斜床 FANUC FOCAS / 宏变量接口说明

### 5.1 这台设备的通信方式
这台设备按你反馈，通信方式为：
- `FANUC FOCAS / 宏变量接口`

### 5.2 现场测试前确认
请客户先确认：
- CNC 已开启 FOCAS 通讯功能
- IP 能 ping 通
- 端口正确（常见是 8193，但以现场参数为准）
- `FWLIB32.dll` 已放置正确
- 宏变量范围已经由现场人员确认

### 5.3 测试流程
建议按以下顺序：
1. 检查网线与 IP
2. 确认端口是否开放
3. 确认 DLL 可用
4. 建链测试
5. 读取状态信息
6. 读取宏变量
7. 写入一个安全的测试宏变量

### 5.4 现场示例
例如客户给出：
- IP：`192.168.1.20`
- 端口：`8193`
- 宏变量：`#1000 ~ #1010`

那么测试时就先从：
- 读 `#1000`
- 再读 `#1001`
- 再写 `#1005`

这种安全范围开始。

---

## 6. 双头镗 Syntec Remote CNC 说明

### 6.1 这台设备的通信方式
双头镗不是 FANUC FOCAS。
它按当前项目定位应为：
- `Syntec Remote CNC`
- 使用 `Syntec.OpenCNC.dll`

### 6.2 客户找不到路径时怎么处理
如果客户不知道“路径”，通常指的是以下几种之一：
- 不知道 SDK DLL 放哪里
- 不知道程序运行时应该引用哪个 DLL
- 不知道设备 IP 和通讯参数
- 不知道宏变量表在哪里

### 6.3 正确的做法
让客户先找这几样：
- 厂家提供的 `Syntec.OpenCNC.dll`
- SDK 示例程序
- 设备 IP
- 宏变量表
- R 区定义

### 6.4 测试顺序
1. 先确认 DLL 在不在
2. 再确认 IP 是否通
3. 再确认程序是否能连上
4. 再读状态
5. 再读宏变量
6. 最后再写一个安全的宏变量

### 6.5 示例
如果客户只知道机器 IP，不知道路径，也可以先做以下工作：
- 让厂家把 SDK 包发来
- 在 SDK 目录里找到 `Syntec.OpenCNC.dll`
- 把 DLL 放到程序同目录

如果还是不清楚，就先让厂家给“示例工程”，因为不同版本的 SDK 文件名可能略有差异。

---

## 7. 客户现场要配合提供的信息

建议客户直接按下面格式填写：

### 设备基本信息
- 设备名称：
- 设备型号：
- 设备厂家：
- 协议类型：
- IP：
- 端口：
- 站号：

### 文件依赖信息
- 需要的 DLL 名称：
- DLL 放置目录：
- 是否有厂家 SDK：
- 是否有示例程序：

### 点位/变量信息
- 读取地址：
- 写入地址：
- 宏变量范围：
- 安全测试位：

---

## 8. 现场排查建议

### 8.1 先别急着写
先做连接测试，再做读取测试，最后才做写入测试。

### 8.2 先确认文件
如果程序提示找不到文件，优先检查：
- DLL 是否存在
- DLL 是否和程序同目录
- DLL 是否被杀毒软件隔离
- 文件名是否拼写错误

### 8.3 先确认网络
如果是网口设备，优先检查：
- IP 地址
- 网关
- 子网掩码
- 端口开放情况

### 8.4 先确认权限
如果是共享目录或文件交换设备，优先检查：
- 文件夹是否共享
- 当前用户是否有读写权限
- 是否有防病毒软件阻止写文件

---

## 9. 建议客户交付的文件清单

### 通用文件
- `AutomaticOnlineHostComputer.exe`
- `AutomaticOnlineHostComputer.dll`
- `AutomaticOnlineHostComputer.deps.json`
- `AutomaticOnlineHostComputer.runtimeconfig.json`
- `客户联调说明.md`

### 斜床 / FOCAS
- `FWLIB32.dll`
- 设备宏变量表
- 设备网络参数表

### 双头镗 / Syntec
- `Syntec.OpenCNC.dll`
- 厂家 SDK 说明
- 宏变量与 R 区说明

### 天车 / PLC
- 控制卡或 PLC 配套 DLL
- 点位表
- 通讯参数表

---

## 10. 如果客户实在不知道怎么找文件
可以直接这样问客户：

1. 这个设备厂家给过你 SDK 包吗？
2. 有没有 `dll` 文件夹？
3. 有没有示例程序？
4. 有没有通信说明书？
5. 有没有 IP、端口、点位表？

如果客户都没有，那就说明：
- 现场资料还不完整
- 需要先向设备厂家补齐资料

---

## 11. 建议的沟通话术
你可以直接告诉客户：

> 当前程序已经准备好联调入口，但需要设备厂家的通讯库文件和点位表。请先提供对应设备的 DLL、IP、端口、站号、宏变量或寄存器表，我们再进行读写测试。

---

## 12. 后续建议
等客户把文件找齐后，可以继续做：
- 联机测试
- 读写测试
- 宏变量测试
- 文件交换测试
- 报警诊断测试

