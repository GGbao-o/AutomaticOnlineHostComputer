# 进度记录

## 2026-07-10

- 开始异常处理、人工恢复与主页面应急按钮审查。
- 已确认工作区存在用户未提交的代码、配置、文档和构建产物；本轮保持只读，不触碰这些改动。
- 已发现既有 `findings.md` 包含相关历史审查记录；后续以当前代码为准复核关键结论。
- 已对照异常审查文档与当前前端、后端、动平衡、研磨及主页面实现。确认前端应急已覆盖“货叉已放板继续”和“_currentWp作废”，但不覆盖机械手/前天车/后天车在途工件的持久身份及统一作废。
- 已确认当前“启动/恢复”只复核天车连续全零；它直接解除前后端暂停，并未完成X11、磁铁、工件所有权、货叉阶段或应急结果的闭环核验。
- 结论：应急恢复不能以“清寄存器 + Resume”为模型，也不能依赖重启进程；应以“暂停派发 → 等旧动作退出 → 人工将设备复位到安全位 → 对指定活动工件执行继续/作废 → 对账完成后才允许恢复派发”为统一事务。现有实现已部分具备该模型，但活动工件上下文和恢复准入门仍未覆盖所有在途路径。
- 用户确认监控页只收集需人工关注的异常、红黄报警和应急操作；不收集高频普通重连、轮询或正常日志。页面数据只保留当前程序生命周期，退出即清空。
- 已新增 AttentionEventCenter（固定上限500条）、异常监控缓存导航页和只读筛选/摘要界面；红色/黄色集中告警与前端、斜床、动平衡、研磨应急结果均写入内存事件中心。首次编译因应急窗口缺少 DI 扩展命名空间失败；补齐后 `dotnet build --no-restore` 成功，0错误。
- 静态核对确认事件记录只出现在 HomeViewModel 的集中告警方法和 EmergencyCenterDialog 的应急结果处理；未改动 FlowEngine、通信客户端、PLC/CNC 写入、任务缓存或设备控制路径。
- 全流程状态页的1号线、2号线和研磨缓存卡片现保留原有数量，并显示最多两条只读摘要；长度缺失时仅显示直径。数据来自原缓存锁内复制，未读取PLC、未修改队列。
