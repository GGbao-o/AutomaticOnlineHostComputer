# 动平衡机械手提前释放位置锁：进度

## 2026-08-23

- 已完成用户确认后的设计说明：`Docs/superpowers/specs/2026-08-23-balancing-manipulator-early-lock-release-design.md`。
- 已创建任务跟踪文件。
- 当前阶段：核对现有实现和测试入口。
- 已确认阈值配置模型与页面落点，下一步核对 M2/M3 当前缓存/结案实现和 `CraneService` 轮询签名。
- 阶段1完成：配置页、运动轮询和现有契约测试入口已核对。
- 阶段2进行中：准备添加两个无范围校验的动平衡 Y 阈值配置字段。
- 阶段2完成：已在 `balancing` JSON、配置模型和配置管理页面增加 M2/M3 提前放锁 Y 字段；未添加任何范围验证。
- 阶段3进行中：为既有绝对移动轮询增加可选的只读状态观察入口。
- 阶段3完成：`MoveAbsoluteAsync` 与 `PollAxesAsync` 已增加可选Y状态观察器；仅复用成功读取的既有轮询状态。
- 阶段4进行中：开始建立M2/M3在途工件状态，并将来源缓存转移、提前放锁和应急恢复统一到该状态。
- 已检查当前工作区差异：发现的配置速度、核心文档措辞和M2释放锁注释均为既有用户修改；实施中仅叠加本任务改动。
- 已确认 `WorkpieceCache` 是值类型、M2/M3 已有动作版本和展示上下文；下一步在同一应急上下文中新增独立在途业务记录并改造缓存转移。
- 已定位 finally、人工账本结案和应急清理的旧缓存语义；它们将与在途记录一起修改，避免释放锁后的新缓存被旧动作处理。
- 已新增受应急锁保护的 M2/M3 在途业务记录、来源缓存原子转移辅助方法，并让在制工件快照优先展示在途业务状态。
- 一次只读命令参数拼写错误，未执行且未影响工作区；已记录并改用分段读取。
- 已复核人工结案与应急清理：人工结案已具备“新缓存不可覆盖”分支，但“人工移走”仍需清理在途记录；应急清理仍需避免删除提前放锁后的新来源缓存。
- 阶段4完成：M2/M3均在X11确认、Z安全后将来源缓存转为在途业务记录；M2在去ST008途中按实际Y提前放锁，M3在ST010回安全途中按实际Y提前放锁；人工结案和应急清理保护后天车新来源缓存。
- 阶段6完成：已详细更新《全线业务逻辑详解》11.3、11.5和11.6，明确在途转移、提前释放、锁范围、无阈值范围校验及人工/应急保护。
- 已运行主项目无还原构建，结果为0错误；项目固有FanucFocas可空性和过时API警告仍存在。
- 阶段5进行中：新增提前放锁契约测试，准备运行相关测试。
- 用户要求根据新给出的 11.1–11.5 完整流程检查当前平衡引擎；已进入阶段8，本轮只读审查，不修改业务代码。
- 2026-08-25：用户确认研磨 FIFO 超限保护采用 `TryDispatchLoadAsync` 的天车锁与派发门闩内检查；开始实现配置、告警、契约测试和业务文档。设计/实现计划已分别记录在 `Docs/superpowers/specs/2026-08-25-grinding-fifo-overflow-dispatch-design.md` 和 `Docs/superpowers/plans/2026-08-25-grinding-fifo-overflow-dispatch.md`。
- 2026-08-25：已添加 `grinding.maxFifoCount=2`、配置页字段和 `TryDispatchLoadAsync` 超限守卫。数量超过上限时引擎在出队前软暂停、通过研磨安全回调弹窗、保留FIFO且由finally释放天车锁。研磨相关12项契约测试通过，解决方案构建成功（0警告、0错误）。
- 已运行平衡/X11/提前放锁/M720 相关测试：共36项，32通过、4失败；正在核对失败是实现问题还是源码字符串契约过时。
- 已确认4个失败均为硬编码默认值/旧文案断言过时；它们仍使当前测试集处于红色，但不否定M720轮询和配置字段的代码存在。
- 已执行全量257项测试：251通过、6失败；编译成功，失败均为当前JSON/文档与旧硬编码契约不一致。

## 2026-08-30

- 开始只读复核前端 POS3 取板异常后的队列/缓存与应急丢板语义，以及研磨天车取板 Z 回升/完成信号的配置延迟时序。
- 已读取既有计划、发现和进度文件；未发现仓库级 `AGENTS.md`、`CLAUDE.md` 或 `GEMINI.md`。
- 已并行委派 PLC/并发流程审查，主线程开始定位具体代码入口与配置。
- 已定位1/2号线 Pos3 天车调度、异常回队列、前端应急入口，以及研磨 M731 配置延迟与并发等待代码；下一步精读相关分支和异常/finally语义。
- 一次检索误带不存在的 `Tests` 路径，已记录并改用实际目录定位测试。
- 已确认运动超时、三次X11=0均不回前天车队列；只有未发送任何物理命令的失败才自动回队。另已定位“人工移走”专用动作结案入口，前端当前工件丢弃与前天车动作结案不是同一个操作。
- 已核对页面按钮：前端在途丢弃负责设备输出/阶段复位；动作账本“已人工移走”负责结清前天车异常任务牌与动作快照。已核对M731时序专用契约测试与配置默认/当前值。
- 已核对研磨异常收尾：M731或Z回升异常会保留PendingWorkpiece并暂停，不回FIFO；准备运行针对性测试。
- 针对性测试完成：9/9通过（前天车人工结案 + M731配置/时序）。
- 一次PowerShell正则引号解析错误未执行检索，已记录并用固定单引号模式成功定位前端安全告警绑定。
- 独立复核并发审查附带风险：来源保留账本被恢复门禁排除；手动页面X11地址63499与生产流程63497不一致，项目文档确认当前生产口径为63497。
- 已独立确认三次X11=0动作会形成 `PauseSourceReserved`，因此确实不会被当前整线恢复门禁拦截；一次Windows通配路径检索错误已记录。
- 阶段10完成：形成两个问题的明确结论、现场处置顺序、相关安全缺口与代码证据；本轮未修改业务代码。针对性测试9/9通过。
- 开始阶段11：专项核对M731“写了但无效”的整字读改写竞争、同字信号冲突和闭环确认问题；只读不改代码。
- 已定位M731调用、MC65同字M721写入者及协议文档描述；下一步精读底层读改写、共享锁、缓存实例和PLC闭环。
- 已确认底层为M720整字读改写；进程内M721/M731由静态同字锁串行，但PLC扫描与外部写入不受锁保护，且M731写后不回读。
- 已完成MC65 M720字内信号映射和全仓库直接写入检索：业务代码未发现绕过API的M720整字写；底层只检查协议ACK，不检查梯形图消费/传送带结果。
- 已确认M731只写1、从不写0或检查先归零，也不等待M730清零；M731若卡1会导致后续写1无新上升沿。已列出同字M721的三个入口。
- 并发审查已回传：同进程M721直接覆盖M731证据不足；根因应优先查M731是否写前已为1、PLC梯形图消费/RST条件、M730是否清零及外部整字写。
- 阶段11完成：已给出整字写语义、同字写入者、覆盖边界和现场判别方法；针对性测试6/6通过，本轮未修改业务代码。
- 开始阶段12：设计M3取得M821/M720两锁后只复查M720；先核对现有收尾路径，再向用户确认设计后实施。
- 一次只读工具编排字符串转义错误导致脚本未执行，已记录并改用固定关键词检索。
## 2026-08-30 M3 锁后复查 M720

- 已核对 M3 锁外派发、两把位置锁取得顺序、动作上下文创建位置、持件后的 M720 等待以及 `finally` 清理路径。
- 结论：可在两把锁取得后、任何来源/物理动作前做一次性 M720 复查；读到0时直接返回，现有 finally 能完整释放锁并清 Busy/页面动作状态。
- 待确认：锁后 M720 通信失败/返回无效时，是否沿用“状态未知即安全暂停”的规则；确认设计后再改业务代码。
- 用户补充：锁后读取异常需要重试。已确认底层读取本身不带重试；下一步向用户确认“3次、500ms间隔、最终失败暂停”的具体设计。
- 用户确认按3次、500ms、最终失败暂停实施并要求补日志。
- 已新增设计说明、实施计划、M3锁后复查方法、控制台/OperationalLog日志、业务文档和3项合同测试。
- TDD红灯：新增3项中2项因锁后方法缺失失败、1项持件后等待通过；绿灯：新增3项全部通过。
- 相邻筛选10项中7通过、3项为HEAD已存在的陈旧合同失败，已只读核实基线，本次未改无关配置或旧测试。
- 最终主项目构建在沙箱外读取本机Microsoft SDK元数据后成功：0警告、0错误。新增M3合同测试最终3/3通过。
- `git diff --check`发现业务文档第802行有一处与本需求无关、用户原有研磨段尾随空格；为保护用户改动未处理，改为对本次M3代码和新增文件独立检查。

## 2026-08-31 未入线任务FIFO暂停重排设计

- 已查看用户截图并定位任务表XAML、`TaskRowViewModel`启停逻辑、`HomeViewModel`全局总上料架FIFO及派发提交边界。
- 已确认根因：暂停行仍占据FIFO原位置，队头暂停会阻塞全队；再启动因重复入队检查而不会移动到队尾。
- 已确认“入线”可靠边界：从全局FIFO出队并成功写入1/2号线前端缓存，页面`AssignedLine`变为1/2；之后不参与待派发顺序。
- 当前阶段只做设计，尚未修改业务代码或页面。
- 用户确认已入线按钮显示“已入线”并禁用，顺序显示“—”；目标聚焦为未入线任务的暂停移除、重新启动入队尾和实时FIFO序号。
- 已补充调度提交窗口设计：出队写线路缓存期间显示“派发中”并禁用，避免暂停与入线竞态。
- 用户批准方案A。已写设计规格`Docs/superpowers/specs/2026-08-31-front-dispatch-task-reordering-design.md`。
- 规格自审已修正派发异常分支歧义：线路缓存写入未成功返回一律原项回队头；成功返回一律进入`EnteredLine`，不得回全局FIFO。未发现TBD/TODO或范围外改造。
- 用户要求开始执行。已创建实施计划`Docs/superpowers/plans/2026-08-31-front-dispatch-task-reordering.md`并完成自审；当前工作区较脏，按既定边界不做Git提交。
- 已先新增队列协调器与任务派发状态测试；目标测试在缺少`FrontDispatchQueueCoordinator`和`TaskDispatchState`时按预期编译失败，TDD红灯成立。
- 首次沙箱内运行目标测试因本机.NET SDK目录访问被拒绝，未修改文件；按权限流程在沙箱外重跑后得到上述预期红灯。
- 已实现`FrontDispatchQueueCoordinator`，统一持有全局FIFO与单个`Dispatching`项；暂停、出队、失败回队头、成功提交均在同一并发锁内完成并生成版本化顺序快照。
- 已将任务行改为`NotQueued/Queued/Dispatching/EnteredLine`显式状态；启动和暂停按钮不再预改状态，派发中显示“派发中”、入线后显示“已入线”且均禁用。
- 已接入Home派发循环：暂停任务从任意FIFO位置移除，再启动入队尾；后台出队后先进入派发提交窗口；只有线路缓存写入成功才设置线路并标记已入线，异常复用原任务项回队头。
- 已在任务表“序号”后增加“顺序”列，待派发任务显示实际1起始FIFO位置，其余状态显示“—”；业务文档3.1/3.2已同步。
- 本需求最终目标测试17/17通过，包含100轮暂停/派发竞争验证；主项目最终构建0警告、0错误。
- 完整测试程序集285项中277通过、8失败；失败均为本任务外的既有配置JSON默认值、研磨告警/动平衡旧文案合同，与本次FIFO协调器、任务状态和页面绑定无关。

## 2026-09-02 研磨目标机参数与XY并行

- 已按用户确认调整天车5持件去目标研磨机的顺序：XY绝对移动与最终直径、孔型、长度参数链并行；参数成功后立即执行数据传输完成3秒信号；`Task.WhenAll`确认两链完成后再做目标X微调。
- 请求上料与门开已移动到目标微调成功之后；之后Z下降、锁紧、退磁、Z安全和上料完成保持原有顺序。
- 并行准备或目标微调异常会自动调用目标机`ClearEmergencyRegistersAsync(CancellationToken.None)`清参数和上位机输出；清理失败同样保留原始异常并记录日志。工件`PendingWorkpiece`保留、不回研磨FIFO，外层既有异常逻辑暂停引擎。
- 已补齐控制台与`OperationalLog`的并行开始、成功、自动清理开始/成功/失败日志，更新12.8业务文档和上料方法步骤注释。
- 新增合同测试首次按预期3/3红灯，实施后3/3绿灯；研磨相邻回归7项中6通过，唯一失败为既有`GrindingM731OverlapContractTests`硬编码JSON应含`m731NotifyDelayMs=1000`，当前用户配置并非该值，与本次改动无关；主项目构建0警告、0错误。
