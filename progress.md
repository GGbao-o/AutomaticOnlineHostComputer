# 进度记录

## 2026-07-10

- 开始异常处理、人工恢复与主页面应急按钮审查。
- 已确认工作区存在用户未提交的代码、配置、文档和构建产物；本轮保持只读，不触碰这些改动。
- 已发现既有 `findings.md` 包含相关历史审查记录；后续以当前代码为准复核关键结论。
- 已对照异常审查文档与当前前端、后端、动平衡、研磨及主页面实现。确认前端应急已覆盖“货叉已放板继续”和“_currentWp作废”，但不覆盖机械手/前天车/后天车在途工件的持久身份及统一作废。
- 已确认当前“启动/恢复”只复核天车连续全零；它直接解除前后端暂停，并未完成X11、磁铁、工件所有权、货叉阶段或应急结果的闭环核验。
- 结论：应急恢复不能以“清寄存器 + Resume”为模型，也不能依赖重启进程；应以“暂停派发 → 等旧动作退出 → 人工将设备复位到安全位 → 对指定活动工件执行继续/作废 → 对账完成后才允许恢复派发”为统一事务。现有实现已部分具备该模型，但活动工件上下文和恢复准入门仍未覆盖所有在途路径。
- 用户确认监控页只收集需人工关注的异常、红黄报警和应急操作；不收集高频普通重连、轮询或正常日志。页面数据只保留当前程序生命周期，退出即清空。
- 已新增 AttentionEventCenter（固定上限500条）、异常监控缓存导航页和只读筛选/摘要界面；红色/黄色集中告警与前端、斜床、动平衡、研磨应急结果均写入内存事件中心。首次编译因应急窗口缺少 DI 扩展命名空间失败；补齐后 `dotnet build --no-restore` 成功，0错误。
- 静态核对确认事件记录只出现在 HomeViewModel 的集中告警方法和 EmergencyCenterDialog 的应急结果处理；未改动 FlowEngine、通信客户端、PLC/CNC 写入、任务缓存或设备控制路径。
- 全流程状态页的1号线、2号线和研磨缓存卡片现保留原有数量，并显示最多两条只读摘要；长度缺失时仅显示直径。数据来自原缓存锁内复制，未读取PLC、未修改队列。

## 2026-07-15

- 开始只读审计：基于当前代码和Docs评估自动线SCADA与轻量MES还可补强的能力，明确不改任何业务逻辑。
- 已恢复既有异常、人工恢复、监控页和流程状态展示的审查上下文；历史发现仅作为线索，后续结论将以当前代码和正式文档复核为准。
- 已完成代码与文档只读取证：确认当前异常监控为500条内存事件、主流程工件进度以UI回调为主、现有数据库工件履历写入未接入当前1/2线主流程；发现无独立测试项目、无角色权限审计、日志输出量大且配置缺少变更追溯。已将改善方向记录为旁路事件/健康/履历/审计/分析层，均不要求改变现有业务握手或动作顺序。
- 记录一次文档更新尝试失败：`findings.md` 的预期锚文本不在文件末尾；已改为按实际末行追加，无生产代码改动。
- 用户确认第一步先实现“运行诊断”页面。已新增不可变诊断快照、HomeViewModel只读聚合、独立页面ViewModel和可见时1秒刷新页面；导航顺序为“全流程状态→运行诊断→异常监控”。
- 诊断聚合只调用现有 `GetFrontDispatchReadiness`、天车/工位/中转架展示快照和 `DeviceStatus` 内存字段；没有新增PLC/CNC/MC/Modbus/FANUC读取、数据库调用或流程控制按钮。页面隐藏/卸载后停止定时器。
- 已同步更新 `Docs/整线技术手册.md` 与 `Docs/日志审查与故障定位手册.md`，明确“等待不等于故障”、页面只读边界和推荐排查顺序。
- 最终完整构建：`dotnet build AutomaticOnlineHostComputer.csproj --no-restore --nologo --consoleloggerparameters:"ErrorsOnly;Summary"` 成功，0错误、660个项目既有警告；未提交Git。
- 用户确认第二步实现“在制工件”页面。已新增不可变在制快照、1/2号线前端只读复制、Home聚合、筛选/KPI页面和缓存导航；导航顺序为“全流程状态→运行诊断→在制工件→异常监控”。
- 动平衡M2/M3只增加带动作版本的显示专用身份，分别在动作里程碑更新、在`finally`按版本清理；静态引用确认这些字段不进入派发、运动、握手、锁、缓存或异常恢复判断。
- 页面仅在可见时每1秒复制内存，内容不变时不重建DataGrid行，隐藏/卸载立即停止；未新增设备通信、数据库、后台Task或控制按钮。
- 已更新 `Docs/整线技术手册.md`、`Docs/日志审查与故障定位手册.md` 和 `Docs/03-设备交互流程/整线引擎流程总览_按代码.md`，补充位置来源、异常前记录步骤及只读边界。
