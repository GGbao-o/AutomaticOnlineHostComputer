# 异常处理、人工恢复与监控页面计划

## 目标

依据 `Docs` 的 catch 异常处理文档及 `Service/FlowEngine`、主页面代码，识别会阻碍“异常后人工安全恢复、放弃当前板、其余任务继续执行”的不合理处理，并给出可实施的恢复模型；设计一个仅保留本次程序生命周期内人工关注事件的异常监控页面。

## 阶段

1. [completed] 定位异常处理文档、应急中心和核心引擎入口。
2. [completed] 对照 catch/finally 与任务、板件、锁和设备状态的实际语义。
3. [completed] 审查主页面应急按钮覆盖范围及恢复后的续跑条件。
4. [completed] 输出按风险分级的结论和建议改造顺序。
5. [completed] 已实现异常、报警与应急操作的内存监控页面，并完成构建与隔离验证。
6. [completed] 已在全流程状态页显示三类缓存的前两块工件直径/长度摘要，并完成构建验证。
7. [completed] 对自动线SCADA与轻量MES现状进行只读能力审计：盘点可观测性、可靠性、数据追溯、运维和性能改进项，不改变业务逻辑。
8. [completed] 实现“运行诊断”只读页面：复用既有内存快照展示前端准入、通信/信号质量、中转架一致性、五台天车任务和人工确认项；页面不可见时停止刷新，并完成正式文档与构建验证。
9. [completed] 实现“在制工件”只读页面：汇总全局FIFO、前后端、动平衡和研磨的当前内存工件位置；仅可见时刷新，多位置/信号不一致只提示不处理；完成三份正式文档与构建、静态隔离审查。
10. [completed] 只读审查全部主要页面的数据来源、刷新链路、失败降级、显示延迟和状态粒度；已形成按风险分级的证据与小改动建议，本阶段未修改生产或页面代码。
11. [completed] 仅审查主导航中未注释的八个页面：逐项核对菜单可达性、页面职责、数据/操作边界与 `Docs` 所述流程的一致性；不修改代码。
12. [completed] 已编写面向上位机工程师的 SCADA+轻量 MES 页面职责与排障导航手册：覆盖八个主导航页面、数据边界、操作边界、故障入口和源码追溯；未修改运行逻辑。
13. [completed] 已为1～5号天车新增Y绝对编码器二次微调：复用X的稳定确认、两次上限、`-1`跳过和超限阻止Z下降；Y默认关闭且全目标为`-1`，未改机械手或Z轴；完整构建为0错误。
14. [completed] 已实现斜床自动上料前的命令输出清理：仅清Modbus 10370~10374或FANUC #1101~#1105；清理失败恢复中转架缓存、目标斜床回Idle并释放后天车锁，天车不会取板；完整构建为0错误。

## 边界

- 已按用户确认的设计实施只读诊断和在制工件页面；后续能力仍需单独设计并确认后再修改。
- 不把现有未提交改动视为本轮产出。
- 新增代码只复制现有内存展示状态；不修改PLC/CNC协议、数据库结构或现有业务规则。

## 本轮审查错误记录

| 错误 | 次数 | 处理 |
|------|------|------|
| 读取`Infrastructure/Services/Management*Service.cs`路径不存在 | 1 | 不重复原命令，改用`rg --files`定位真实路径后继续只读审查 |
| PowerShell中组合`rg`模式时双引号未闭合 | 1 | 不重复原命令，改为单引号模式并拆分查询；未执行任何源码操作 |
| `rg`查询包含不存在的`Views/EquipmentOverview`目录 | 1 | 保留已返回的其他匹配，后续使用真实目录`Views/Overview`；未修改源码 |
| 第二次组合`rg`正则因PowerShell参数转义失败 | 1 | 改为多个简单`-e`模式完成定位，不重复复杂正则 |
| Y微调首次构建报CS8361 | 1 | 为插值字符串中的条件表达式加括号后重新构建 |
| 斜床命令清理首次构建报Line2 SkewCtx无M服务 | 1 | 2号线全部为FANUC，删除不存在的Modbus分支后重新构建 |
| 命令清理静态核对组合rg正则转义失败 | 1 | 改用多个简单`-e`匹配完成调用顺序核对 |
