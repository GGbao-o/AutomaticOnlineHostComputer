# 动平衡机械手提前释放位置锁任务计划

## 目标

实现机械手2去 ST008 途中、机械手3从 ST010 回安全位途中按实际 Y 越过配置阈值提前释放位置锁；来源缓存必须在取走确认后转为动作在途工件，旧动作不得删除后天车新写入的同名缓存。

## 阶段

1. [completed] 核对现有配置、配置页面、运动轮询、M2/M3 状态机和相关测试契约。
2. [completed] 增加配置模型与配置页面字段；不增加阈值范围校验。
3. [completed] 在天车绝对 Y 运动的既有轮询中提供只读进度观察。
4. [completed] 改造 M2/M3 来源缓存到在途工件、提前释放和异常/人工结案保护。
5. [in_progress] 增加/更新契约测试并构建、运行相关测试。
6. [completed] 更新《全线业务逻辑详解》机械手2、机械手3完整动作及相关异常说明。
7. [pending] 最终差异审查、验证和交付；不提交 Git。
8. [in_progress] 对照用户给出的 11.1–11.5 完整契约逐项复核当前工作区实现，输出“符合/部分符合/不符合”与代码证据；不修改业务代码。
9. [completed] 在研磨上料派发门内增加可配置 FIFO 超限软暂停、主页面安全告警、业务文档和回归契约；不取消已启动动作、不写PLC、不删除FIFO。
10. [completed] 只读核对前端天车 POS3 取板异常后的队列/缓存归属、前端应急丢板路径，以及研磨取板 Z 回升与取料完成信号的配置延迟时序；输出代码证据与现场处置前提，不修改业务代码。
11. [completed] 只读审查 MC65 的 M731 写入实现：确认位写/整字读改写语义、M720～M735同字内所有写入者、PLC与上位机并发覆盖窗口、客户端锁与重试范围，并解释“M731每次写但无效”的可能原因；不修改代码。
12. [completed] 实施M3取得M821/M720两锁后的M720单项复查：M720=0正常退出并由finally释放资源；读取异常3次、间隔500ms重连，最终失败暂停；已完成代码、日志、文档、目标测试和主项目构建验证。
13. [completed] 实施主页面未入线任务FIFO顺序管理：暂停原子移出FIFO、再次启动进入队尾；派发中/已入线禁止调序；主页面显示真实顺序；已完成并发测试、集成契约、文档和主项目构建验证。

## 不可违反的业务边界

- M2 仅在持板去 ST008 的既有状态轮询得到实际 Y 小于阈值后释放本趟 M817/M818 锁。
- M3 仅在 ST010 放料、M721、研磨 FIFO 闭环后，回安全 Y 的既有状态轮询得到实际 Y 小于阈值后释放 M720、M821 锁。
- X11=1 且 Z 安全到位才可把来源缓存转为在途工件；转移本身不释放锁。
- 阈值不做范围、上下限或关联坐标校验。
- 正常提前释放、finally 和应急释放必须幂等；提前释放后旧动作不得删除新来源缓存。

## 错误记录

- 读取人工结案代码时有一次工具脚本括号拼写错误；未执行命令、未影响文件，已改用分段只读检查。
- 2026-08-23 全量复核运行 36 个相关测试，32 通过、4 失败：两个配置 JSON 字符串断言、一个文档原文断言、一个 M720 日志原文断言失败。待判定是契约测试过时还是配置/文档实现未同步。
- 2026-08-30 一次研磨关键字检索包含不存在的 `Tests` 路径，`rg` 返回路径不存在；有效源码/配置结果仍已返回，后续改为先定位实际测试目录，不重复该路径。
- 2026-08-30 一次 PowerShell 中双引号正则被外层命令解析导致 `ParserError`；未执行检索、未修改文件，已改用单引号固定模式并成功定位告警绑定。
- 2026-08-30 一次 `rg` 在Windows下把 `FlowAction*.cs` 当作无效字面路径并返回os error 123；同次显式文件检索已返回所需结果，后续不使用该通配路径。
- 2026-08-30 M3设计核对时一次JS工具编排字符串含未转义反斜杠，脚本在解析阶段失败、未执行任何命令；已改用不含正则反斜杠的固定关键词检索。
- 2026-08-31 初次检索误带不存在的 `Models` 路径，并有一个PowerShell正则因引号截断为未闭合分组；有效的XAML结果已返回，后续改用固定关键词分次检索并成功定位任务队列代码。
- 2026-08-31 未入线FIFO目标测试首次在沙箱内运行时因本机.NET SDK目录访问被拒绝；按权限流程在沙箱外重跑，测试因待实现的协调器和状态枚举缺失而按预期红灯。
