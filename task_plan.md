# 当前任务：按天车方向修正 Y 绝对编码器微调

# 当前任务：异常监控页面显示与详情说明

## 目标

按当前源码说明异常监控页面左侧会显示的事件类型、筛选与聚合语义，以及点击事件后右侧实际展示的完整证据、恢复和操作指引；只读审查，不修改代码。

## 阶段

- [x] 恢复异常监控现有实现与页面结构
- [x] 核对事件目录、事件模型和右侧格式化字段
- [x] 输出面向现场操作的页面使用说明与当前边界

## 审查边界

- 以当前源码为准，不把尚未接入生产调用点的事件类型表述成必然会出现。
- 不修改页面、事件采集或生产流程。

---

# 当前任务：按天车方向修正 Y 绝对编码器微调

# 当前任务：研磨机磨石报警单机禁派发与显眼状态

## 目标

将西门子 TypeA 的磨石1/2厚度报警从“暂停研磨引擎”改为“仅该研磨机停止自动分配”，保留取料前工件回FIFO，其他研磨机继续运行；设备总览和全流程状态显示显眼报警状态；同步业务与协议文档。

## 阶段

- [x] 核对截图、现有暂停路径、调度筛选和页面状态来源
- [x] 用户确认单机禁派发设计
- [x] 固化设计与实施计划
- [x] 增加回归契约并完成引擎/页面实现
- [x] 更新业务、协议、技术和异常手册
- [x] 完成完整测试、构建与差异检查

## 已确认决策

- 磨石报警只阻止报警的 TypeA 机台接收新的上料任务，不暂停研磨引擎。
- TypeA 扫描保存两路报警；正式取料前继续二次读取，竞态报警仅退缓存和释放锁。
- 已加工或等待下料的机台不因该维护信号中断下料。
- 每次报警进入/恢复记录一次，持续报警最多一分钟心跳一次。
- 设备总览和全流程状态使用 `⚠ 磨石N厚度报警 · 已停止自动分配` 红色状态；不增加PLC写操作。

---

# 当前任务：按天车方向修正 Y 绝对编码器微调

## 目标

把 Y 显示坐标与 D5020 的方向关系改为按天车配置，并让固定工位微调、斜床动态下料目标和移动后反馈校验共用同一个方向。

## 阶段

- [x] 核对现场数据与现有代码
- [x] 固化设计与实施计划
- [x] 修改配置模型、配置文件和公共微调助手
- [x] 更新两条后天车动态下料调用及业务文档
- [x] 验证 JSON、方向公式和完整构建

## 已确认决策

- Crane#2（1号线后天车）方向为 `-1`：显示 Y 与 AbsY 反向。
- Crane#4（2号线后天车）方向为 `+1`：显示 Y 与 AbsY 同向。
- 动态目标统一为 `中心AbsY - 方向 * yOff`。
- 微调显示目标统一为 `当前显示Y + 方向 * (目标AbsY - 当前AbsY)`。
- Crane#1/#3/#4 已由现场图片确认同向 `+1`；Crane#2确认反向 `-1`。
- Crane#5尚未确认，暂时保留原有 `-1` 行为且未标定工位继续以目标`-1`跳过。
- 本次不写入尚未最终确认的斜床中心 AbsY 标定值。

## 工作区边界

- 保留已有未提交文件和构建产物，不回退、不清理。
- 只修改本任务相关源文件、源配置和文档。

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 首次文档补丁锚点不匹配 | 1 | 拆分补丁并改为在计划文件末尾追加任务 |
| 方向文档检索命令的PowerShell引号转义失败 | 1 | 改用单引号模式并移除JSON双引号检索，复查通过 |

---

# 当前任务：运行诊断、在制工件、异常监控页面审查

## 目标

结合 `Docs` 业务流程和实际数据链路，审查三个页面的定位、数据正确性、新鲜度、异常恢复辅助能力、交互语义、并发刷新及性能风险；只给审查结论，不修改业务代码。

## 阶段

- [x] 定位三个页面及其导航、ViewModel、数据模型和事件中心
- [x] 阅读相关流程、异常恢复和设备交互文档
- [x] 逐页核对数据来源、刷新生命周期、状态语义和边界场景
- [x] 交叉核对后台引擎状态、锁/缓存/设备信号对页面结论的影响
- [x] 验证关键问题并输出按严重程度排序的审查报告

## 范围约定

- “三个页面”指 `runtimeDiagnostics`、`inProcessWorkpieces`、`attentionMonitor`。
- `flowStatus` 仅作为共享数据与展示语义参照，不作为本轮主审页面。
- 保留工作区已有未提交内容；除规划记录外不写文件。

---

# 当前任务：异常监控覆盖率与异常描述专项审计

## 目标

从生产代码所有异常出口反向核对 `AttentionEventCenter` 覆盖情况，重点审查X/Y绝对编码器微调、充磁/退磁/X11、运动、通信、握手、被动等待、重试与直接弹窗路径；输出详细异常覆盖矩阵和结构化描述规范，不修改业务代码。

## 阶段

- [x] 梳理异常监控现有入口、去重和刷新链路
- [x] 逐调用点审计X/Y微调及其外围catch结果
- [x] 审计前天车、后天车、机械手、动平衡、研磨的磁铁/X11异常
- [x] 汇总其他只写日志、自动重试、吞异常、无超时路径
- [x] 与Docs异常手册交叉核对，形成覆盖矩阵与详细事件字段规范
- [x] 完成证据复核并输出按风险排序的报告

## 审查口径

- “监控到”必须实际调用 `AttentionEventCenter.Record`，仅写Console、暂停或弹MessageBox均不算。
- 分开记录：异常是否发生、是否暂停、是否弹窗、是否进入异常监控、是否保留工件/磁铁/位置证据。
- 普通瞬时通信重试可以不弹窗，但最终失败、物理结果未知、动作可能已执行或长期等待必须形成可追溯事件。

---

# 当前任务：生产异常监控结构化升级实施计划

## 目标

依据已确认的 `Docs/superpowers/specs/2026-07-20-operational-event-monitor-design.md`，编写一份可逐任务执行、测试驱动、逐文件明确的实施计划；本阶段只输出计划，不修改生产代码。

## 阶段

- [x] 确认书面设计和不可变业务边界
- [x] 映射现有项目结构、测试设施和异常页面依赖
- [x] 定位首批监控接入点及准确方法边界
- [x] 编写分任务实施计划、测试样例和提交节点
- [x] 对照设计进行覆盖、占位符和类型一致性自审
- [x] 验证并交付实施计划

## 不可变边界

- 计划中的新增调用不得改变任何分支条件、状态赋值、缓存、工件所有权、暂停、恢复、重试、超时、锁和设备命令。
- Reporter不得为了补详情读取PLC/CNC、数据库或文件；调用点只复制已掌握的局部证据。
- 页面只读；只允许查看、筛选、复制和用户主动CSV/TXT导出。
- 工作区已有未提交内容全部保留，不纳入计划文件的提交步骤。

---

# 当前任务：生产异常监控结构化升级实施

## 执行方式

- 用户选择子代理分任务执行。
- 每个任务由独立执行代理负责明确文件范围，完成后由另一代理做规格/代码审查，主代理运行最终验证。
- 任务按 `Docs/superpowers/plans/2026-07-20-operational-event-monitor.md` 的依赖顺序执行。

## 当前阶段

- [x] Task 1：建立测试工程和仓库定位助手
- [x] Task 2：定义强类型事件、证据和监控配置
- [x] Task 3：实现Store、Reporter、聚合和阈值状态机
- [x] Task 4：事件目录、详细文案和CSV/TXT导出
- [x] Task 5：兼容Attention事件并注册依赖
- [x] Task 6：向七个引擎注入Reporter
- [x] Task 7：修复弹窗去重漏记并接入集中最终异常
- [x] Task 8：接入22个微调调用点
- [ ] Task 9：接入充磁、退磁、X11和物理承诺点（执行中）
- [ ] Task 10：接入50个PressureStop事件
- [ ] Task 11：通信失败、恢复和阶段滞留观察
- [ ] Task 12：主从ViewModel、筛选和无漏刷刷新
- [ ] Task 13：页面与导航新增数量
- [ ] Task 14：完整业务隔离复核和回归验证

---

# 当前任务：后天车上料 X11 三次为 0 异常链专项复核

## 目标

核对 1/2 号线后天车从中转架取板时，`XY到位 -> XY微调 -> Z下降 -> 充磁 -> X11三次为0 -> Z回升 -> 退避/释放锁 -> 暂停引擎` 是否符合现场要求；本轮只审查，不修改业务代码。

## 阶段

- [x] 读取附件时间线、业务文档和相关历史提交
- [x] 核对 1/2 号线 `DoLoad` 的取板、catch 和 finally
- [x] 核对 ZoneMT/ZoneTS/后天车锁释放与安全告警回调
- [x] 运行静态验证和构建/测试，形成最终结论

## 审查口径

- X11 连续三次有效返回 0 后必须进入异常路径。
- 异常路径必须先退磁并让 Z 回到安全位置，再允许 X 退避。
- 无论目标是否为 ST108/ST606，异常后都必须释放本动作持有的锁并暂停对应整线，禁止自动进入下一轮重试。
- 保留工作区已有未提交内容，不修改生产业务逻辑。

---

# 当前任务：生产异常监控页面业务隔离与手册一致性审查

## 目标

核对异常监控页面是否真正只读、是否会阻塞或改变生产执行，以及用户提供的 v1.0 手册中容量、聚合、筛选、详情、导出、导航红点和事件覆盖是否与当前代码一致；本轮只审查，不修改业务代码。

## 阶段

- [x] 恢复既有实施进度并定位页面完整依赖链
- [x] 审查 Store/Reporter 的并发、锁、容量和生产线程开销
- [x] 审查 ViewModel/View 的刷新、筛选、详情、导出和只读边界
- [x] 审查导航红点生命周期及页面异常隔离
- [x] 对照手册逐项形成问题清单并运行验证

## 审查口径

- 页面和页面服务不得读取或写入 PLC/CNC、数据库、业务缓存、任务或引擎控制状态。
- Reporter 调用不得传播异常到生产流程；Store 锁内不得执行UI、文件I/O或设备I/O。
- 页面刷新、筛选、格式化、复制和导出不得持有生产锁或阻塞设备通信。
- “已经实现”必须以当前源码和测试为准，不以计划或手册描述为准。

---

# 当前任务：后天车上料X11三次失败最终结构化闭环

## 目标

不改变1/2号线后天车上料任何设备动作、异常分支、缓存、锁、暂停和弹窗逻辑；把X11三次有效为0的结构化上报移动到finally全部收尾完成后，一次性记录退磁、Z回安全、退避、锁释放和整线暂停结果。

## 阶段

- [x] 核对当前X11失败与finally收尾证据
- [x] 确认采用finally完成后单次上报方案
- [x] 写入设计与实施计划
- [x] 先写失败测试并验证红灯
- [x] 实现1/2号线对称最终事件
- [x] 更新业务文档和异常监控手册
- [x] 完成全量测试、构建和差异审查

## 不可变边界

- 第三次X11=0的判断、异常文本和抛出行为不变。
- 退磁、不可取消Z回0、Z安全才退避、锁释放、暂停和弹窗顺序不变。
- Reporter不得读取PLC/CNC、不得返回业务判断、不得传播异常。
- 只生成一条`CRANE_X11_NOT_CONFIRMED`最终事件，不用聚合次数表达处理阶段。

---

# 当前任务：生产流程异常安全专项复核

## 目标

只读检查六个核心生产引擎及公共调度的异常路径，重点确认异常后不会在Z低位/位置未知时执行X或Y水平运动，不会在物理状态未知时自动重试或进入下一轮，并核对锁释放、工件身份和对应引擎暂停是否安全。

## 阶段

- [ ] 梳理所有PressureStop、通用catch、finally运动和自动重试入口
- [ ] 逐流程核对Z、磁铁、X11、工件、锁和暂停状态
- [ ] 核对整线暂停回调与主循环重新派发条件
- [ ] 汇总独立任务流程审查结果并复核证据
- [ ] 运行静态验证、测试和构建并输出风险报告

## 审查口径

- 只要Z已经下降、命令可能已发送或当前位置无法确认，禁止任何X/Y横移，除非先有不可取消的Z安全动作成功返回或位置复核。
- 充磁、退磁、X11或运动结果未知时，不得把工件当作确定仍在来源位或确定已放入目标位。
- 发生需要人工对账的异常后，应暂停实际拥有该设备/共享区域的对应引擎；释放软件锁不等于设备已离开碰撞区。
- 本轮只审查，不修改生产代码。

---

# 当前任务：全线业务与异常处理文档对照审查（只读）

## 目标

以 `Docs/全线业务逻辑详解.md` 和“发生异常以后程序到底怎么办：大白话说明”对应的异常/人工恢复文档为准，追踪到当前代码，标出实现与文档不一致、安全和可恢复设计缺口。不修改生产代码。

## 阶段

- [x] 确认审查对象、工作区状态与只读边界
- [x] 提取两份文档的主线、异常分类与声明的处置语义
- [x] 按流程引擎、共享锁、工件账本、暂停/恢复链路对照源码
- [x] 复核关键证据并输出按严重程度排序的审查报告

---

# 当前任务：机械手2取料判定、M720分流等待与后天车安全修复

## 目标

修复：1) M817/ST020 取板触发与缓存判定；2) 1号线后天车下料分流时 M720 临时不可放料的等待行为；3) M817路径同时检查2、4号后天车的 X 安全范围；同步更新 Docs 业务逻辑文档并完成回归验证。

## 阶段

- [x] 定位 Docs 中的机械手2、M817/M818/M823、M720 与后天车安全区规约
- [x] 追溯三项对应的当前代码分支、缓存、日志与配置读取
- [x] 固化已确认的修复设计与实施计划
- [x] 实现三项业务修复与定向回归测试
- [x] 更新 Docs 业务逻辑文档并完成全量验证

---

# 当前任务：动平衡机械手按搬运任务设置速度与流程文档补全

## 目标

机械手2、机械手3每趟新的自动搬运在首次物理移动前按当前配置写一次三轴绝对速度；同一趟任务的后续 Y/Z、充退磁和 X11 重试不重复设置。同步按源码复核并补全动平衡引擎业务逻辑文档。

## 阶段

- [x] 核对 M2/M3 初始化、恢复、任务入口、九寄存器写入与实际来源/目标
- [x] 固化已确认设计和实施计划
- [x] 先添加失败契约测试
- [x] 实现任务级速度设置并移除初始化一次性设置
- [x] 补全全线业务逻辑详解和流程总览
- [x] 完成全量测试、构建和差异检查

## 已确认决策

- 一趟 M2/M3 搬运只设置一次速度，不在每次 Y/Z 运动、充退磁或 X11 重试前重复写入。
- 速度设置发生在来源/缓存/握手/X11 残留检查通过后、首次 `MoveAbsoluteAsync` 前；失败时尚未运动，按既有异常收尾释放软件锁和 busy。
- 以每趟任务设置覆盖 PLC 残留速度；暂停恢复本身不额外写，以免恢复后首任务重复下发。
# 当前任务：近期业务修复的并发与文档一致性复核（仅审查）

- [x] 复核机械手2/3、M817/M818、M720 等待与后天车安全范围的锁顺序、取消和缓存回退。
- [x] 复核研磨机磨石报警的单机禁派发、扫描并发与恢复逻辑。
- [x] 对照 Docs 中的业务逻辑、异常监控说明与实际代码，记录不一致处。
- [x] 按风险等级给出死锁、长期阻塞、竞态和文档修订建议；本轮不改代码。

---

# 当前任务：全链路业务、通信与异常安全复核（仅审查）

## 目标

以 `Docs` 的业务规约为预期、当前生产源码与通信服务为事实，系统审查前后天车、机械手、动平衡、研磨和通信层的锁顺序、等待/取消、位置安全、物理承诺点、异常回退、暂停/恢复与页面监控覆盖；不修改业务代码。

## 阶段

- [x] 建立锁、等待、动作任务牌与通信串行化的全局图谱。
- [x] 审查全部 `PressureStopException` 与运动/磁铁异常；确认存在P0：未知位置仍可自动续跑/横移。
- [x] 审查缓存、PLC/CNC握手和通信重试的幂等性与物件所有权；发现P1：副作用写缺少响应未知后的状态对账。
- [x] 对照 Docs 主流程、协议、异常手册与实际实现，已记录下压、重试、Z回升和阶段监控的不一致。
- [x] 按严重级别输出死锁、饥饿、竞态、安全与可观测性结论；本轮不改代码。

---

# 当前任务：全体天车绝对运动超时安全暂停与恢复

## 目标

将任一自动流程中天车 X/Y/Z 绝对运动未在规定时间内到位统一提升为安全异常：停止后续派发、暂停所属引擎、记录并弹出可操作的异常；物理区域位置未知时不自动重试、不清或恢复工件缓存，也不错误释放碰撞区锁。默认 XY/Z 单动作超时为 120 秒，并同步更新业务逻辑和天车通信文档。

## 当前阶段

- [x] 复核 2 号线后天车中转架 XY 超时的现有捕获、缓存和锁路径
- [x] 确认用户采用“统一 120 秒 + 引擎安全暂停 + 人工恢复”的策略
- [x] 固化各引擎共用的超时异常、区域安全占用与人工恢复设计
- [x] 实现公共120秒阶段超时、D4518停止证据与天车服务配置注入
- [x] 实现后天车位置未知安全占用；前天车/研磨天车暂停且禁止自动回队列/FIFO
- [x] 补充 Docs 业务逻辑、设备交互和天车通信协议文档
- [x] 完成定向测试、全量测试、构建和差异复核（全量172/172；构建0警告0错误）

## 已确认边界

- `±5mm` 容差维持不变；任一参与轴超过容差且到达超时即视为未到位。
- 超时后必须复位运动触发并采用现场认可的停止/急停命令；不得继续 Z、磁铁、握手或下一轮派发。
- 不能无条件释放 ZoneMT/ZoneTS 等物理碰撞区锁；位置未知时应以安全占用保留到人工确认。
- 缓存、在途工件和锁的最终处置必须由人工恢复结果决定，而不能由异常分支自动猜测。

---

# 当前任务：全线业务逻辑文档的内存账本补充

## 目标

按当前生产源码补全 `Docs/全线业务逻辑详解.md`，明确各段缓存/在途工件/任务牌的写入、判断、所有权转移、正常清理、异常保留和应急清理边界；不修改业务代码。

## 阶段

- [x] 核对前端全局 FIFO、线路缓存、机械手/货叉状态机、前天车队列和现有前端应急
- [x] 核对后端中转架/斜床、动平衡位置缓存、研磨 FIFO 与单机 Pending 的生命周期
- [x] 更新业务主文档并标注现有前天车恢复能力缺口
- [x] 完成文档差异和关键文字复核

---

# 当前任务：后天车上料完整业务动作状态机（第一阶段）

## 目标

将 1/2 号线后天车“中转架取料→斜床上料”从局部 `FlowActionContext` 迁移为完整的、以物理事实为唯一依据的动作账本：逐步骤记录命令确定性、工件归属、坐标和持锁；统一决定等待、暂停和人工处置；异常监控显示完整上下文。其余引擎不在本阶段重写。

## 阶段

- [x] 复核附件设计与当前半迁移实现，确认范围只覆盖 1/2 号线后天车上料。
- [x] 固化第一阶段设计与逐项实现计划。
- [x] 为动作账本、预约保留、异常结论和事件映射补充失败测试。
- [x] 扩展 `FlowActionContext`，补齐真实工件、XYZ/X11/磁铁、锁及可区分的人工暂停结论。
- [ ] 统一所有动作状态机的人工结论、人工恢复账本和异常详情结构。
- [ ] 完整迁移 1/2 号线后天车上料、下料每一个物理/握手步骤及其局部补偿。
- [ ] 迁移 1/2 号线前天车、机械手1、机械手2/3与研磨天车的搬运动作。
- [ ] 把动作快照接入异常监控详情与人工处置入口；四种人工结论必须实际驱动账本。
- [ ] 更新全线业务主文档、权限业务逻辑、设备交互和通信协议，使其仅陈述当前源码行为。
- [ ] 完成全量测试、构建、文档/代码一致性与锁/异常路径复审。

## 已确认边界

- 物理命令尚未发送的读取、就绪和目标不可放料失败可正常等待重试；已发送命令后的超时、下压越界、通信响应未知必须暂停，禁止自动重试。
- 软件锁在动作收尾释放；来源缓存不在 X11=1 前删除，但处于“待人工确认”的未知动作不能被新的自动任务重新选取。
- 局部 catch 只执行已经证明安全的退磁/Z 回安全补偿；统一 catch 只根据账本得出最终业务状态；finally 不再决定业务恢复或发送新的正常业务命令。
- 不在本阶段改变 PLC 地址、正常动作顺序、斜床协议或其它引擎的业务流程。

---

# 2026-07-29：文档与当前源码对齐修订（完成）

- [x] 按当前源码复核前天车 P0 自动重入队列和后天车下料内部暂停但不通知主页的两类例外。
- [x] 更新 `Docs/全线业务逻辑详解.md`：明确目标统一规则与当前例外的边界、真实的暂停/弹窗/UI表现及现场处置。
- [x] 更新 `Docs/搬运动作状态机_异常处理与人工恢复审查说明.md`：补充 P0 的触发条件、待人工快照与自动重试并存、前后端暂停范围和后天车 UI 状态例外。
- [x] 完成 Markdown 差异与一致性检索；本次未修改任何生产 `.cs` 源码。

---

# 2026-07-29：统一天车异常裁决器第一批修复（完成）

- [x] 新增 `FlowActionExceptionResolver`，将最终异常收敛为 `WaitRetry` 或 `PauseLineManual`。
- [x] 修复两条前天车：已发物理命令后不再重入 `_craneQueue`，统一暂停并走主页安全回调。
- [x] 修复两条后天车下料：X11 未确认的响应未知建立 `pendingSafetyAlarm`，最终走主页安全回调。
- [x] 修复天车通信层：读取保持三次重连；寄存器/DINT 写入改为单次尝试，禁止盲重放。
- [x] 增加主页恢复前的待人工动作账本门禁。
- [x] 新增回归测试并完成构建、全量测试、源码复查和文档同步。
