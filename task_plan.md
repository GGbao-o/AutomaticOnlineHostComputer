# 全线异常恢复与任务阻塞风险审查

## Goal

系统审查整线业务逻辑中异常、取消、应急停止与重启后的状态恢复路径，识别可能遗留在进程内存或设备侧、从而永久阻塞后续任务执行的状态，并提供有代码依据的风险清单与整改建议。

## Current Phase

Phase 9 — 全线业务逻辑核心文档重构

## Phases

### Phase 1: 范围与入口梳理
- [x] 核实工作区变更和项目约定
- [x] 定位整线引擎、任务调度、设备客户端及异常恢复入口
- **Status:** complete

### Phase 2: 异常状态写入与清理审计
- [x] 梳理快照、占用、任务标志和 PLC/设备写入点
- [x] 对照正常完成、异常、取消、应急停止、重启路径的清理行为
- **Status:** complete

### Phase 3: 调度准入与永久阻塞风险审计
- [x] 检查各设备/天车在任务启动时的拒绝条件
- [x] 追溯每个拒绝条件的清除责任与可达性
- **Status:** complete

### Phase 4: 交叉验证与分级
- [x] 检查调用链、finally 块、异常传播和持久化边界
- [x] 形成可复现的风险场景、影响和修复方向
- **Status:** complete

### Phase 5: 审查报告
- [x] 输出按严重度排序的发现与证据位置
- [x] 标注需现场确认的设备/PLC 语义
- **Status:** complete

### Phase 6: 取放料源码流程说明

- [x] 逐段核对机械手、后天车斜床和研磨天车的真实准入及动作步骤
- [x] 将核对结果写入《全线业务逻辑详解》，采用编号步骤，不使用表格
- **Status:** complete

### Phase 7: 主页面内存状态复位

- [x] 为斜床实现仅复位 `St` 和 `Wp` 的单床入口
- [x] 为研磨机实现仅复位 `State` 和 `PendingWorkpiece` 的单机入口
- [x] 增加主页入口、设备选择窗口与范围契约测试
- **Status:** complete

### Phase 8: 研磨取料参数显示屏实施

- [x] 编写实施计划和失败优先测试
- [x] 增加显示屏 IP 配置、配置页面和专用 Modbus 通信组件
- [x] 在补长、X11 与 M730 二次确认后接入旁路写入
- [x] 更新通信矩阵、研磨流程和业务说明文档
- [x] 运行聚焦测试、全量测试与主项目构建
- **Status:** complete

### Phase 9: 全线业务逻辑核心文档重构

- [x] 核对五台天车及上下游设备的当前源码流程
- [x] 重写主文档结构并合并分散说明
- [x] 逐步骤补全调度、状态机、锁、工件所有权和异常恢复
- [x] 移除主文档中的表格、重复编号和日期补丁式章节
- [x] 完成静态一致性验证
- **Status:** complete

## Key Questions

1. 哪些内存状态、快照、队列项或设备寄存器会影响任务准入？
2. 它们是否在正常完成、异常、取消、应急处理和进程/线路重启时均能清除？
3. 抛出异常后，是否存在清理代码被跳过、清理再次失败或清理顺序错误的路径？
4. 是否有能够永久阻止天车或其他设备接受任务的残留条件？

## Decisions Made

| Decision | Rationale |
|---|---|
| 仅审查，不改动业务代码 | 当前请求是风险分析；避免在未确认 PLC 现场语义前改变控制行为。 |
| 以“状态写入点 → 清除责任 → 任务准入读取点”为审计主线 | 可直接证明残留状态是否会阻塞后续调度。 |
| 显示屏通信是旁路输出 | 连接或写入失败只记研磨日志，不改变研磨状态、缓存、PendingWorkpiece 或安全告警。 |
| 本阶段不创建 Git 提交 | 用户明确要求实施和更新文档，但不需要提交。 |
| 主文档全篇使用编号步骤，不使用表格 | 用户明确要求每一个步骤按 1、2、3 说明，并将该文件作为唯一核心流程文档。 |

## Errors Encountered

| Error | Attempt | Resolution |
|---|---:|---|
| apply_patch 上下文不匹配 | 1 | 读取当前计划后改用精确上下文更新，未修改业务代码。 |
| 新实施计划补丁中一行缺少 `+` 前缀 | 1 | 文件未创建；缩短补丁并确保新增文件每行都有补丁前缀。 |
| 全量测试有3条既有契约失败 | 1 | 聚焦测试8/8与主项目构建通过；失败来自本任务未改动的X11/微调JSON默认值及M3日志文案，保留并在交付中披露。 |
| findings追加补丁上下文使用了截断文本 | 2 | 两次均未修改文件；读取准确行内容后完成追加。 |
