# 打标机通信协议（基于文件交换的握手）

## 核心机制
上位机与打标机通过监控共享文件夹内的特定文件实现状态同步和任务触发。不使用标准工业协议。

## 共享文件夹
- **路径**：【待补充，例如 `\\192.168.x.x\LaserShare\`】
- **权限**：双方均需读写权限

## 交互流程

### 1. 查询打标机状态
- 上位机读取文件夹中某个文件内容
- `1` → 空闲，可发起新任务
- `0` → 忙碌 / 有版，需等待

### 2. 发起打标任务
- **前提**：状态为空闲，且工件已到位，天车已退回安全位
- **动作**：上位机在文件夹内 **生成文件 `A`**（无后缀）
- **文件内容**：