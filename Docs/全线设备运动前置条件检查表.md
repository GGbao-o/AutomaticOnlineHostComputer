# 全线设备运动前置条件检查表

> 生成日期: 2026-06-02
> 涵盖: 1号线前端/后端、2号线前端/后端、平衡引擎、研磨引擎

---

## 零、总上料架全局分线

| 动作 | 前置条件 | 检查/周期 | 说明 |
|------|---------|---------|------|
| **全局FIFO队头派发到1/2号线** | ① 全局FIFO有队头任务 ② M800=1 ③ 不在M800复位门 ④ 任务行已启动未暂停 ⑤ 版长满足候选线路斜床能力 ⑥ 大直径强制1号线规则未被违反 ⑦ 线路前端准入通过 | 全局调度循环1秒一次 | 只绑定当前物理板和软件队头，不跳过后续任务 |
| **线路前端准入** | ① 线路运行且未暂停 ② 本线前端缓存为空 ③ `_currentWp=null` ④ `_forkPhase=Idle` ⑤ 货叉快照≤3秒且待机无板 ⑥ 双头镗快照≤3秒且R6101=1 | 只读内存快照；不读PLC、不拿动作锁 | R6103不作为准入条件，避免分线前死等 |
| **两线都可接时选线** | 两条线均满足斜床能力和前端准入 | 压力少优先；平压交替 | 压力=前端缓存+`_currentWp`+前天车队列+前天车活动标志 |
| **写入线路缓存前二次确认** | 已选中线路 | 再读一次该线准入快照 | 若二次准入失败，任务仍保留在全局FIFO队头 |

---

## 一、机械手1（1号线 + 2号线共用）

| 动作 | 触发引擎 | 前置条件 | 超时 |
|------|---------|---------|:--:|
| **XY→ST007 取料** | Line1Front / Line2Front | ① `_manipulatorLock` 已获取 ② M800=1(总上料架有板) ③ 缓存有工件数据 ④ `_currentWp=null`(无在途) ⑤ `_forkPhase=Idle`(货叉空闲) ⑥ `_boringSvc`已连接且 R6101=1(请求数据；SkipBoring 也要求) ⑦ `_boringSvc`未连接→**保守拒绝取料** | MoveAsync 240s |
| **Z↓台面(下压检测)** | Line1Front / Line2Front | Z 下探过程中 `PressureStopException` 视为正常停止 | 每次3mm×2步=最多6mm |
| **充磁** | Line1Front / Line2Front | Z 已到达取料位(下压触发或最深) | — |
| **X11检测(充磁→3s→查X11)** | Line1Front / Line2Front | 充磁完成。失败→退磁→Z↓5mm→充磁重试，最多2次 | 3s×2=6s |
| **Z↑0(取料后)** | Line1Front / Line2Front | 充磁成功(X11=1) | — |
| **XY→ST711/712 送叉** | Line1Front / Line2Front | ① Z=0(已升顶部) ② 货叉在待机位且**无版**(防止已有工件碰撞)；M801在放板、Z回0和Y到首次安全点之后才发送 | MoveAsync 240s |
| **Z↓叉台面→退磁** | Line1Front / Line2Front | XY 已到位。下压触发=正常 | — |
| **Z↑0 → Y→货叉释放安全位** | Line1Front / Line2Front | 退磁完成；1线Y=11500、2线Y=1000，到位后写M801并打开门闩 | — |
| **1线Y→取板待机1000** | Line1Front | 货叉门闩已开；继续持有共享机械手锁，和货叉握手并行 | — |

### 机械手1 不参与的操作

- 机械手1 **不直接参与**天车Pos3取料、双头镗握手、打号机、中转架

---

## 二、货叉（1号线 ST711 / 2号线 ST712）

| 动作 | 前置条件 | 信号交互 |
|------|---------|---------|
| **待机→双头镗送料并回待机** | ① `_forkPhase=GoingToBoring` ② `_forkHandshakeInProgress=false` ③ `_boringSvc`已连接→R6103=1(请求上料) ④ 当前任务机械手放行门闩已开 | 写M912=1→等M901=1且M900=0 |
| **待机→拉伸Pos3(天车位/SkipBoring)** | ① `_forkPhase=SkipBoring_GoPos3` ② 当前任务机械手放行门闩已开 | 写M914=1→等AtPos3 |
| **双头镗取料并自动到Pos3** | ① `_forkPhase=WaitingMachine` ② R6107=1(请求下料) | 写M913=1→等M902=1且M900=1 |
| **Pos3→缩回待机位（天车取完通知）** | 天车Pos3取料完成后通知 | ProcessCraneFrontAsync 中写M911=1 |

### 货叉分派触发条件（进入 Load_WriteParams）

1. `fs.HasPlate=true` — 待机位传感器检测到工件
2. `fs.AtStandbyPos=true` — 货叉在待机位
3. `_currentWp!=null` — 引擎有在途工件数据
4. `_forkPhase=Idle` — 货叉状态机空闲
5. `_manipulatorClearedFork=true` — 当前任务已完成退磁放板、Z回0、本线首次安全Y及M801；1号线机械手可继续回1000

---

## 三、双头镗（Modbus TCP，R区地址=R*2+1）

### 取总上料架前判断

| 寄存器 | 要求 | 含义 |
|--------|:--:|------|
| R6101 | =1 | 双头镗请求数据，允许前端进入下一块工件节拍；正常工件和 SkipBoring 都要求 |

### 上料握手

| 步骤 | 引擎→CNC | CNC→引擎 | 说明 |
|:--:|---------|---------|------|
| 1 | — | R6101=1 | CNC就绪，请求数据 |
| 2 | 写R2041长度、R2043直径、R2044左堵厚、R2045右堵厚、R2046锥度、R2047版孔、R2048圆角 | — | R2041写16位无符号整数原值；其余按任务值*100写入；锥度和圆角均须>0且<=655.35 |
| 3 | R6102=1 | — | 数据下发完成；本轮R6108置1后清0 |
| 4 | — | R6103=1 | CNC请求上料（货叉可伸Pos2） |
| 5 | R6104=1 | — | M912送料回待机后写，上料完成，双头镗开始加工 |

### 下料握手

| 步骤 | 引擎→CNC | CNC→引擎 | 说明 |
|:--:|---------|---------|------|
| 1 | — | R6107=1 | CNC请求下料（加工完成） |
| 2 | M913=1 | — | 货叉取料并自动到Pos3 |
| 3 | R6108=1 | — | M902=1且M900=1后写，下料完成 |

---

## 四、天车1（1号线前天车）— Line1Front

| 动作 | 前置条件 | 安全检查 | 超时 |
|------|---------|---------|:--:|
| **Pos3取料** | ① `_craneFrontLock` 获取 ② `_craneQueue` 有工件 ③ `X11=0`(磁铁无残留) ④ XY去Pos3前先确认前天车Z=0，若不在0位则先回0（误差5mm） ⑤ X11检测(充磁→3s→查X11→重试)；当前不再等待机械手1安全Y，因现场确认ST711/ST712与Pos3不重叠 | X11地址: 63497；Z=0检查失败时禁止XY横移 | 2次×3s |
| **Z↑安全高度** | Pos3取料成功(X11=1) | 配置文件 `safeZHeight`；与M911回待机并行启动，但后续流程必须等待回升完成 | — |
| **通知货叉回待机** | Pos3取料成功(X11=1) | 现场确认Pos3低位持板时货叉回待机不会碰天车/工件/取料夹具；与Z回升并行写M911=1，任一失败暂停保现场 | — |
| **打号机路径** | 前天车已取得 `ZoneMT`（打号机 ↔ 中转架碰撞区） | 后天车不能进入中转架；ST108/ST606侧只需`ZoneTS`的动作不被阻塞 | — |
| **打号机：Z↓退磁放下** | `ZoneMT`已持有 | `ST107` 坐标 + 公式Z | — |
| **打号机：Z↑+写A.txt（并发）** | 退磁完成 | 写 `\\192.168.2.67\1\A.txt` | — |
| **打号机：等B.txt** | A.txt已写 | 轮询 `B.txt` | 1小时（3600s） |
| **打号机：Z↓充磁取回+X11** | B.txt出现 | 同Pos3取料逻辑 | 2次×3s |
| **中转架放料** | ① 进入中转架前取得`ZoneTS`，此时持有`ZoneMT+ZoneTS` ② 中转架有空位(M811/M812/M813) ③ Z↓退磁放下 ④ `OnRackPlaced` 回调通知后端 | 中转架物理读写必须在`ZoneMT+ZoneTS`内完成 | — |
| **归位** | 放料完成 | X→`CraneHomeX[1]`, Z→`_craneOffsetZ` | — |
| **释放碰撞区锁** | 放料完成且Z安全后的finally/正常收尾 | 释放`ZoneTS`再释放`ZoneMT` | — |

---

## 五、后天车2（1号线后天车）— Line1Rear

### 取料上料（DoLoad）

| 动作 | 前置条件 | 锁 | 超时 |
|------|---------|:--:|:--:|
| **选斜床+中转架** | ① `_craneRearLock`获取(非阻塞) ② 斜床Idle+RqData/RqLoad ③ 中转架物理有版 ④ `_wps`有缓存数据 ⑤ 长度匹配 | crLock；真正进中转架时再取Zone | — |
| **进入中转架** | `DoLoad`内部按`ZoneMT -> ZoneTS`获取 | 拿锁后重新复核物理有板+缓存存在+斜床可用 | — |
| **Z→0** | 进入中转架前确保Z安全/归零 | — | — |
| **XY→中转架** | Z已归零 | — | — |
| **Z↓取料→充磁→X11** | XY已到位。X11=1后Z↑安全高度 | — | 2次×3s |
| **取料完成释放Zone锁** | Z已在安全高度且X11确认有板 | 普通斜床释放`ZoneTS+ZoneMT`；ST108/ST606释放`ZoneMT`并保留`ZoneTS` | — |
| **XY→斜床** | 普通斜床Zone锁已释放；ST108/ST606仍持`ZoneTS` | — | — |
| **上料握手** | XY已到斜床上方 | — | 120s |

### 下料分流（DoUnload）

| 动作 | 前置条件 | 锁 | 超时 |
|------|---------|:--:|:--:|
| **Z→0 → XY→斜床 → Z↓→充磁→X11** | ① `_craneRearLock`获取 ② `bed.St=WaitingUnload` ③ 目的地空闲确认 ④ X11=0磁铁检查 | crLock | — |
| **下料握手(清信号→等尾座张开)** | X11=1充磁成功 | — | 120s |
| **Y回→Z↑安全** | 尾座已张开 | — | — |
| **分流检查目的地(M817/M720)** | — | 对应分流位置锁（如`_lockM817`/`_lockM720`，仅当前动作持有后释放） | — |
| **长板→ST019放料** | M817空闲 + `_lockM817`获取 | balLock | — |
| **短板→ST010放料** | M720=1允许放料 + `_lockM817`、`_lockM720`获取 + 拿锁后二次确认M720 | 分流位置锁 | 代码当前未调用WaitManipulators，依靠位置锁隔离 |
| **通知回调** | 退磁完成 | OnBalancingRackPlaced / OnGrindingRackPlaced | — |
| **归位** | balLock释放 | X→`CraneHomeX[2]` | — |

---

## 六、天车3（2号线前天车）— Line2Front

与天车1逻辑相同，差异：

| 项目 | 天车1 (1号线) | 天车3 (2号线) |
|------|-------------|-------------|
| 编号 | CraneNo=1 | CraneNo=3 |
| Pos3站号 | ST713 | ST714 |
| 打号机 | ST107 (192.168.2.67) | ST502 (192.168.2.74) |
| 中转架 | ST105/ST101/ST106 | ST016/ST017/ST018 |
| 中转架寄存器 | M811/M812/M813 | M814/M815/M816 |

---

## 七、后天车4（2号线后天车）— Line2Rear

与后天车2逻辑相同，差异：

| 项目 | 后天车2 (1号线) | 后天车4 (2号线) |
|------|---------------|---------------|
| 编号 | CraneNo=2 | CraneNo=4 |
| 斜床 | ST108~ST112 | ST606~ST610 |
| 协议 | ST112=FANUC, 其余Modbus | 全部FANUC |
| 长板目的地 | ST019/M817 | ST020/M818 |
| 短板目的地 | ST010/M720 | ST021/M821 |
| 短板目的地检查 | MC65 M720=1（ST010允许放料） | MC63 M820=1（ST021允许后天车放料） |
| 2号线后端握手快照 | — | `DropRackSnapshotValid=true`，否则本轮禁止向ST020/ST021派发 |

---

## 八、天车5（研磨天车）— Grinding

### 上料（ProcessWorkpieceAsync）

| 动作 | 前置条件 | 检查 | 超时 |
|------|---------|------|:--:|
| **扫描研磨机** | 检查 `IsRequestDataAsync()`；TypeA同步读取磨石1/2报警 | GrinderState=Idle + LastRequestData=true + 无磨石报警 | 500ms |
| **写工件参数** | ① 等请求数据=1 ② 磨石无报警(TypeA，报警机停止自动分配但不暂停研磨引擎) ③ D200读长度(如Length=0) | SetDataSentDoneAsync | 120s |
| **XY→ST709取料** | X11=0(磁铁无残留) + MC65 M730=1，动作前再次确认；XY横移前先确认研磨天车Z=0，若不在0位则先回0（误差5mm） | 63497 / M730 / Z=0 | — |
| **充磁→X11检测** | Z已到公式位置 | 63497, 2次重试 | 3s×2 |
| **M731=1通知PLC** | X11=1吸到 | MC65写731=1 | — |
| **Z↑安全** | M731已写 | `safeZHeight`(绝对Z) | — |
| **XY→研磨机** | Z已安全 | — | — |
| **等请求上料+门开** | XY已到位 | `IsRequestLoadAsync`, `IsDoorOpenAsync` | 120s |
| **Z↓装料** | 请求上料=1 + 门开 | 公式 `grinderZ-d/2` | — |
| **上料到位→等锁紧→退磁→Z↑→上料完成** | clear-before-set | SetLoadInPlaceAsync→IsClampDone→MagnetOff→Z↑→SetLoadDoneAsync | 120s |

### 下料（UnloadFromGrinderAsync）

| 动作 | 前置条件 | 检查 | 超时 |
|------|---------|------|:--:|
| **等门开** | `GrinderState=WaitingUnload` | `IsDoorOpenAsync` | 120s |
| **XY→研磨机→Z↓充磁+X11** | ① 门开 ② MC64下料架=`M720=1`(无板且允许放料，主循环已查) ③ X11=0 ④ XY横移前先确认研磨天车Z=0，若不在0位则先回0（误差5mm） | 63497 / Z=0, 2次重试 | 3s×2 |
| **下料到位→等松开** | X11=1充磁成功 | SetUnloadInPlaceAsync→IsUnclampDoneAsync | 120s |
| **Z↑→XY→ST710** | 尾座已松开 | — | — |
| **Z↓退磁→Z↑→M721** | XY到下料架 | Z升安全后MC64只写M721=1 | — |
| **下料完成** | — | SetUnloadDoneAsync, 研磨机→Idle | — |

---

## 九、机械手2 — Balancing (M2Flow)

| 动作 | 前置条件 | 锁 | 检查 |
|------|---------|:--:|------|
| **等待位置锁** | `_m2Busy=false + _m2`已连接 + (M817有板，或M823=1且M818缓存存在) | `_lockM817`/`_lockM818` | ST020来源需后天车4号X≤safeX |
| **检查M710允许放料** | `M710=1`，派发前快照与拿锁后二次确认均必须成立 | — | 读失败按未知处理 |
| **等缓存就绪(≤5s)** | M817/ST020来源都必须有对应软件缓存；ST020缓存键仍为`M818` | — | 10次×500ms |
| **ST020取料完成** | X11确认吸住且Z升安全后写M824=1 | `_lockM818` | 写失败暂停 |
| **X11检查磁铁** | X11=0 | — | 63497 |
| **Y→取料位 → Z↓→充磁+X11** | 坐标从armCoords配置或数据库读取 | myLock | X11 2次重试 |
| **Z↑安全 → Y→ST008 → Z↓退磁** | X11=1充磁成功 | myLock | safeZHeight |
| **M711=1通知PLC** | 退磁完成 | myLock | MC65写711=1 |
| **Y→安全位** | M711已写 | myLock | `Manipulator2SafeY` |
| **finally释放** | _m2Busy=false, myLock?.Release | — | — |

---

## 十、机械手3 — Balancing (M3Flow)

| 动作 | 前置条件(useM700=true) | 前置条件(useM700=false) | 锁 |
|------|----------------------|------------------------|:--:|
| **来源** | M700=1(动平衡料架2有板) | M825=1且`_balWps["M821"]`存在(ST021允许取料) | — |
| **后天车检查** | 无(无天车干涉) | 天车4 X≤safeX | — |
| **ST010允许放料检查** | M720=1，拿锁后再次读取 | M720=1，拿锁后再次读取 | — |
| **位置锁** | `_lockM821`+`_lockM720` | `_lockM821`+`_lockM720` | 先M821后M720，释放顺序相反 |
| **直径来源** | MC65读D100 | `_balWps["M821"]`缓存(≤5s) | — |
| **取料YZ** | `GetArmCoord("M700"/"M821")` | 同上 | — |
| **放料YZ** | `GetArmCoord("M720", "ST010")` | 同上 | — |
| **来源取料完成** | M700来源写M701=1 | ST021来源写M826=1 | X11确认且Z升安全后 |
| **M721=1通知PLC** | 放到ST010并Z升安全后MC65写721=1 | 同上 | `_lockM720` |
| **通知研磨引擎** | `OnGrindingRackPlaced(d,1,0/len)` | 同上 | — |
| **finally释放** | `_m3Busy=false`, lockPlace?.Release, lockPick?.Release | 同上 | — |

---

## 十一、斜床握手（锦州 Modbus / FANUC FOCAS）

### 上料握手信号（DoHandshake）

| 步骤 | Modbus(锦州) | FANUC(FOCAS) | 方向 |
|:--:|-------------|-------------|:--:|
| 1 | 10370=0(清加工方式) 10373=0(清尾座张开) | SafeSetMacro(#1101,0) | 写 |
| 2 | 写加工参数(直径/版长/堵孔) | SendMachiningParamsAsync | 写 |
| 3 | 10371=1(尾座夹紧) | SetCraneLoadInPlaceAsync(true) → #1101=1 | 写 |
| 4 | 等10371确认 | IsTailstockClampedAsync → #1003 | 读 |
| 5(退磁后) | 10371=0(清夹紧) → 10372=1(远程启动) | SafeSetMacro(#1102,0) → SetCraneLoadDoneAsync | 写 |

### 下料握手信号（DoUnload）

| 步骤 | Modbus(锦州) | FANUC(FOCAS) | 方向 |
|:--:|-------------|-------------|:--:|
| 1 | 10372=0(清远程启动) 10371=0(清夹紧) | SafeSetMacro(#1103,0) | 写 |
| 2 | 10373=1(尾座张开) | SetCraneUnloadInPlaceAsync(true) → #1103=1 | 写 |
| 3 | 等10373确认 | IsTailstockOpenedAsync | 读 |
| 4(Z↑后) | 10373=0(清) → 10373=1(下料完成) | SafeSetMacro(#1104,0) → SetCraneUnloadDoneAsync | 写 |

---

## 十二、相邻碰撞区互斥（SafetyFlags）

| 锁 | 获取方 | 区域 | 释放点 |
|------|--------|------|------|
| `ZoneMT` | 前天车打号；前/后天车进入中转架 | 打号机 ↔ 中转架 | 前天车中转架放料Z安全；后天车取中转架Z安全 |
| `ZoneTS` | 前/后天车进入中转架；后天车ST108/ST606上下料 | 中转架 ↔ ST108/ST606 | 中转架放/取料Z安全，或ST108/ST606退避/异常收尾 |

**规则**: 中转架位于两个碰撞边界之间，必须同时持有 `ZoneMT + ZoneTS`。禁止出现“已持有 `ZoneTS` 再等待 `ZoneMT`”的反向顺序。

---

## 十三、Zone锁与位置锁用途

| 操作 | 引擎 | 持锁方式 | 保护内容 |
|------|------|---------|---------|
| 放板 | Line1Front/Line2Front | 进入中转架前持有`ZoneMT+ZoneTS` | 中转架PLC寄存器 + 物理位置 |
| 取板(DoLoad) | Line1Rear/Line2Rear | `ZoneMT -> ZoneTS` | 中转架PLC寄存器 + 物理位置 + 后端缓存复核 |
| 下料分流(DoUnload) | Line1Rear/Line2Rear | `WaitAsync(ct)`等待对应位置锁 | 1号线M817/M720，2号线M818/M821区域保护 |
| 缓存读写 | 全部 | 配合`_wpLock`/`_balWpsLock` | 工件数据一致性 |

---

## 十四、信号地址速查

| 信号 | 地址 | PLC | 读取者 | 含义 |
|------|------|-----|--------|------|
| X11(天车磁铁) | 63497 | 各天车PLC | 全部引擎 | 1=有工件在磁铁上 |
| M800 | M800 bit0 | 192.168.2.63 | 前端引擎 | 1=请求取料 |
| M801 | M801 | 192.168.2.63 | 前端引擎写 | 取料完成脉冲 |
| M817 | M800字2 bit1 | 192.168.2.63 | Balancing | 1=ST019有版 |
| M818 | M816字 bit2 | 192.168.2.63 | Line2Front/Rear | 1=ST020允许后天车放料 |
| M819 | M816字 bit3 | 192.168.2.63 | Line2Rear写 | ST020后天车放料完成 |
| M820 | M816字 bit4 | 192.168.2.63 | Line2Front/Rear | 1=ST021允许后天车放料 |
| M821 | M816字 bit5 | 192.168.2.63 | Line2Rear写 | ST021后天车放料完成 |
| M823 | M816字 bit7 | 192.168.2.63 | Balancing | 1=ST020允许机械手2取料 |
| M824 | M816字 bit8 | 192.168.2.63 | Balancing写 | ST020机械手2取料完成 |
| M825 | M816字 bit9 | 192.168.2.63 | Balancing | 1=ST021允许机械手3取料 |
| M826 | M816字 bit10 | 192.168.2.63 | Balancing写 | ST021机械手3取料完成 |
| M811 | M800字 bit11 | 192.168.2.63 | 前端引擎 | 1号线中转架1有版 |
| M700 | M688字 bit12 | 192.168.2.65 | Balancing | 1=动平衡料架2有版 |
| M710 | M704字 bit6 | 192.168.2.65 | Balancing | 1=ST008允许放料 |
| M720(MC65) | M720字 bit0 | 192.168.2.65 | Balancing/Line1Rear | 1=ST010允许放料 |
| M730(MC65) | M720字 bit10 | 192.168.2.65 | Grinding | 1=ST709末位有版可取 |
| M720(MC64) | M720字 bit0 | 192.168.2.64 | Grinding | 1=ST710无板且允许放料 |
| M711 | M711 | 192.168.2.65 | Balancing(M2Flow)写 | 送工件到ST008 |
| M721 | M721 | 192.168.2.65 | Balancing(M3Flow)/Line1Rear写 | 放料完成→传送带 |
| M721(MC64) | M721 | 192.168.2.64 | Grinding写 | 下料放版完成 |
| M731 | M731 | 192.168.2.65 | Grinding写 | 取料完成 |
| D100 | D100 | 192.168.2.65 | Balancing(M3Flow)读 | M700工件直径 |
| D200(MC65) | D200 | 192.168.2.65 | Grinding读 | 2号位长度测量 |
