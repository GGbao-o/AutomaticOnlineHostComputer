# 天车 / 机械手运动逻辑与安全机制

> 生成日期：2026-05-13  
> 适用范围：全项目天车(1~5号)、机械手(1~3号)所有运动操作

---

## 一、机械手 X 轴禁用的六层防护

| 层 | 机制 |
|---|------|
| **UI 层** | `IsCraneSelected = false` → X±/X回原点/充磁/退磁/接液盘按钮 `IsEnabled=False` |
| **VM 命令层** | `MoveAxisAsync('X')` 检测到机械手 → 直接 `return` 不执行 |
| **VM 命令层** | `HomeXAsync()` / `MagnetOnAsync()` / `MagnetOffAsync()` / `DrainOpen/CloseAsync()` → 全有 `IsCraneSelected` 守卫 |
| **VM 绝对移动层** | `MoveAbsoluteAsync` 强制 `xTarget = -1` |
| **引擎调度层** | `MoveManipulatorToStationAsync` 只读 Y/Z 坐标，X 不读 |
| **PLC 层** | `MoveAbsoluteAsync` 中 `xTarget < 0` → 不写 D3102/D3103，不触发 D4522，轮询永远判 OK |

## 二、安全机制清单

| # | 机制 | 实现位置 | 作用 |
|---|------|---------|------|
| 1 | **任务前安检** | `CheckSafetyAsync` | 检查忙闲(D5006)、故障(D5009)、伺服报警(D5011)、有版(D5029)，四项全通过才放行 |
| 2 | **手动模式守卫** | `EnsureManualModeAsync` | 所有手动操作前先写 D4001=2 + D4500=2，急停除外 |
| 3 | **Z 下降安全顺序** | `MoveAbsoluteAsync` | Z↓：先走 XY → XY 到位 → 再走 Z（防工件撞下方设备） |
| 4 | **Z 上升安全顺序** | `MoveAbsoluteAsync` | Z↑：先走 Z → Z 到位 → 再走 XY（防低高度拖拽） |
| 5 | **接液盘互锁** | `MoveAbsoluteAsync` | Z↓前自动开液盘，Z↑后自动关液盘 |
| 6 | **防重入** | `AsyncRelayCommand` | 按钮命令执行中 `CanExecute=false`，防止连点重复写寄存器 |
| 7 | **2→0 脉冲规范** | 全部触发寄存器 | 写 2 触发 → 写 0 复位，PLC 不卡在触发态 |
| 8 | **超时复位** | `MoveAbsoluteAsync` finally | 绝对移动无论成功超时都在 finally 复位触发 |
| 9 | **到位补偿** | `PollAxesAsync` | 首次未到位自动补偿一次重试 |
| 10 | **预约互斥** | `ProductionFlowEngine` | 天车/机械手去工位前先预约，防止两台同时去同一工位 |
| 11 | **工位等待** | `WaitForStationAsync` | 工位被占最多等 30s，超时放弃 |
| 12 | **退磁容错** | `MagnetOffAsync` | 10次失败→充磁再退磁，总上限50次(~25s) |
| 13 | **运动范围约束** | 配置文件 `start_x/end_x` | 数据库已有字段，引擎可校验（待接入） |

## 三、运动寄存器速查

### 绝对移动（到指定坐标）

| 步骤 | X | Y | Z | 协议 |
|------|------|------|------|------|
| 写目标 | D3102~D3103 | D3104~D3105 | D3106~D3107 | FC16 DINT |
| 触发 | D4522=2→0 | D4521=2→0 | D4520=2→0 | FC06 INT |
| 读位置 | D5018~D5019 | D5022 | D5025 | FC03 |
| 速度 | D2501~D2503 | D2510~D2512 | D2519~D2521 | FC06 |

### 相对运动（手动点动一个轴）

| 步骤 | 寄存器 |
|------|--------|
| 距离 | D4501 |
| 方向(1正/2负) | D4502 |
| X触发 | D4505=2→0 |
| Y触发 | D4504=2→0 |
| Z触发 | D4503=2→0 |

### 手动操作区完整列表

| D编号 | 常量名 | 功能 | 写入值 |
|--------|--------|------|--------|
| D4001 | `D_ModeCmd` | 自动/手动切换 | 2=手动 |
| D4500 | `D_ManualMode` | 手动操作使能 | 2 |
| D4501 | `D_ManualDistance` | 相对运动距离 | 步长值 |
| D4502 | `D_ManualDirection` | 运动方向 | 1正/2负 |
| D4503 | `D_ManualMoveZ` | Z相对运动触发 | 2→0 |
| D4504 | `D_ManualMoveY` | Y相对运动触发 | 2→0 |
| D4505 | `D_ManualMoveX` | X相对运动触发 | 2→0 |
| D4506 | `D_ManualHomeZ` | Z回原点触发 | 2→0 |
| D4507 | `D_ManualHomeY` | Y回原点触发 | 2→0 |
| D4508 | `D_ManualHomeX` | X回原点触发 | 2→0 |
| D4510 | `D_ManualMagnetOn` | 充磁触发 | 2→0 |
| D4511 | `D_ManualMagnetOff` | 退磁触发 | 2→0 |
| D4512 | `D_ManualDrainOpen` | 接液盘开触发 | 2→0 |
| D4513 | `D_ManualDrainClose` | 接液盘关触发 | 2→0 |
| D4514 | `D_ManualClearAlarm` | 清除报警触发 | 2→0 |
| D4515 | `D_ManualServoPowerOff` | 伺服断电触发 | 2→0 |
| D4516 | `D_ManualServoPowerOn` | 伺服通电触发 | 2→0 |
| D4517 | `D_ManualSlowStop` | 减速停止触发 | 2→0 |
| D4518 | `D_ManualEStop` | 急停触发 | 2→0 |
| D4520 | `D_ManualZAbsMove` | Z绝对移动触发 | 2→0 |
| D4521 | `D_ManualYAbsMove` | Y绝对移动触发 | 2→0 |
| D4522 | `D_ManualXAbsMove` | X绝对移动触发 | 2→0 |
| D4523 | `D_PressureEStop` | 下压急停状态（PLC写1，主机写0清除） | 清除=0 |

## 四、机械手 vs 天车能力矩阵

| 能力 | 天车 | 机械手 |
|------|:---:|:---:|
| X 轴点动 ± | ✅ | ❌ |
| Y 轴点动 ± | ✅ | ✅ |
| Z 轴点动 ± | ✅ | ✅ |
| X/Y/Z 回原点 | ✅ | Y/Z 可，X ❌ |
| X/Y/Z 绝对移动 | ✅ | Y/Z 可，X 跳过 |
| 充磁/退磁 | ✅ | ❌ |
| 接液盘开/关 | ✅ | ❌ |
| 抖动松料 | ✅（X轴） | ❌ |
| Z 步进下探 | ✅ | ✅ |
| 急停/减速停止 | ✅ | ✅ |
| 伺服通/断电 | ✅ | ✅ |
| 清除报警 | ✅ | ✅ |

## 五、Z 轴特殊逻辑

```
Z ↓ 下降（取料/放料场景）：
  开接液盘 → 等500ms → 触发XY绝对移动 → 等XY到位 → 触发Z绝对移动 → 等Z到位
  ↘ 到位后如果没检到版 → StepDownUntilPickupAsync（每次降5mm，最多10次）

Z ↑ 上升（取料后提起）：
  触发Z绝对移动 → 等Z到位 → 触发XY绝对移动 → 等XY到位 → 关接液盘

Z 不变（水平移动）：
  XYZ同时触发 → 等全部到位
```

## 六、速度体系

| 场景 | 速度寄存器 | 配置来源 | 默认值 |
|------|-----------|---------|:---:|
| 绝对移动（三轴同步） | D2501~D2503 / D2510~D2512 / D2519~D2521 | `motion_settings.json` absMove | 500 mm/s |
| 相对点动（三轴同步） | D2504~D2506 / D2513~D2515 / D2522~D2524 | `motion_settings.json` relMove | 300 mm/s |
| Z 快慢两段 | — | 配置已预留（zAxis），代码待接入 | — |

## 七、绝对移动完整流程

```
MoveAbsoluteAsync(xTarget, yTarget, zTarget)
│
├─ 1. EnsureManualModeAsync: D4001=2, D4500=2
├─ 2. 写目标坐标: D3102~D3103 (X), D3104~D3105 (Y), D3106~D3107 (Z)
├─ 3. 读当前 Z，判断升降方向
│
├─ 4. Z↓ → 开接液盘(D4512=2→0)
│
├─ 5. 触发顺序:
│   ├─ Z↓: 触发XY → 等XY到位 → 触发Z
│   ├─ Z↑: 触发Z → 等Z到位 → 触发XY
│   └─ Z不变: 同时触发XYZ
│
├─ 6. 轮询 D5018/D5022/D5025，等全部到位(容差±5)
│   └─ 首次未到位 → 补偿重试一次
│
└─ 7. finally: 复位全部触发(D4520~D4522=0)
    └─ Z↑ → 关接液盘(D4513=2→0)
```

## 八、引擎自动调度流程

```
MoveCraneToStationAsync(craneNo, stationCode)
│
├─ 1. 查 station_coords 缓存 → 读 x/y/z 坐标
├─ 2. 预约工位 → 已被占? → 等待30s → 超时放弃
├─ 3. CheckSafetyAsync → 不通过? → 返回失败
├─ 4. SetAbsSpeedAsync → 写9个速度寄存器
├─ 5. MoveAbsoluteAsync → 执行绝对移动(含Z轴安全逻辑)
├─ 6. 读到位坐标 → 偏差日志
└─ 7. finally: 释放工位预约 + 位置写库
```

## 九、配置参数

修改 `Config/motion_settings.json` 无需改代码，重启生效：

| 段 | 关键参数 | 默认 | 含义 |
|----|---------|:---:|------|
| absMove | defaultSpeed | 500 | 绝对移动速度(mm/s) |
| absMove | tolerance | 5 | 到位容差(mm) |
| absMove | timeoutMs | 30000 | 移动超时(ms) |
| relMove | defaultSpeed | 300 | 相对点动速度(mm/s) |
| zAxis | fastSpeed | 500 | Z快速段速度(预留) |
| zAxis | slowSpeed | 100 | Z慢速段速度(预留) |
| safety | magnetOffMaxRetries | 50 | 退磁最大重试 |
| shake | defaultAmplitude | 5 | 抖动振幅(mm) |
| engine | stationWaitTimeoutMs | 30000 | 工位等待超时(ms) |

## 十、研磨机自动连接

启动时自动连接4台研磨机（ST701~ST704），每5s轮询状态并更新全厂状态卡：

| 研磨机 | 站号 | 类型 | 读的寄存器 |
|--------|------|------|-----------|
| 研磨机1 | ST701 | 新代 TypeB | R7308(状态), R7301(请求数据), R7306(加工中), R7307(门开) |
| 研磨机2 | ST702 | 新代 TypeB | 同上 |
| 研磨机3 | ST703 | 西门子 TypeA | 40001 DI: bit0(报警), bit14(加工中), bit9(请求数据) |
| 研磨机4 | ST704 | 西门子 TypeA | 同上 |

IP 从数据库 `machine` 表读取，无 IP 则跳过。连接失败自动退避重连（5s→30s）。

## 十一、待完成的安全项

| # | 功能 | 状态 |
|---|------|:---:|
| 1 | Z 轴快慢两段下降 | 配置已预留，代码待接入 |
| 2 | 磁铁下压限位自动刹车 | 待确认 DI2 的 Modbus 映射 |
| 3 | 天车运动范围超限校验 | 数据库 `start_x/end_x` 已有，引擎待接入 |
| 4 | 天车回零检查 | 绝对值编码器，正常运行不需要 |
